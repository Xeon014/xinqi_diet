# 小程序接口对接说明

## 基础约定

- 所有接口统一使用 `/api` 作为前缀。
- 所有响应统一使用 `ApiResponse<T>`。
- 小程序侧优先判断 HTTP 状态码，其次判断 `code` 是否为 `SUCCESS`。
- 除登录接口外，业务接口统一携带 `Authorization: Bearer {accessToken}`。

统一响应结构：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {},
  "timestamp": "2026-03-07T23:40:00"
}
```

## 认证与用户

### 1. 微信登录并自动建档

`POST /api/auth/wechat/login`

请求：

```json
{
  "code": "wx.login返回的code",
  "clientUserKey": "客户端本地用户键(开发 mock 可选)"
}
```

关键返回字段：

- `accessToken`
- `userId`
- `isNewUser`

当前小程序调用方：

- `app.onLaunch`
- `services/auth.js`

### 2. 查询当前用户详情

`GET /api/users/{userId}`

关键返回字段：

- `name`
- `gender`
- `birthday`
- `height`
- `currentWeight`
- `targetWeight`
- `bmi`
- `bmr`
- `dailyCalorieTarget`
- `goalMode`
- `goalCalorieDelta`

当前小程序调用方：

- `pages/profile`
- `pages/personal-info`
- `pages/health-profile`

### 3. 更新用户资料

`PUT /api/users/{userId}`

说明：

- 只更新请求体中传入的字段；未传字段保持原值。
- `useFormulaBmr=true` 时后端会清空 `customBmr`，改为公式计算口径。
- 字段可不填，不要求一次性补齐。

请求示例：

```json
{
  "name": "小林",
  "height": 168.5,
  "currentWeight": 58.2,
  "useFormulaBmr": true
}
```

当前小程序调用方：

- `pages/personal-info`
- `pages/health-profile`
- `pages/onboarding-profile`

### 4. 预览目标方案

`POST /api/users/{userId}/goal-plan-preview`

关键请求字段：

- `currentWeight`
- `targetWeight`
- `targetDate`
- `dailyCalorieTarget`
- `goalCalorieStrategy`
- `useFormulaBmr`

关键返回字段：

- `recommendedDailyCalorieTarget`
- `recommendedGoalCalorieDelta`
- `plannedWeeklyChangeKg`
- `warningLevel`
- `warningMessage`
- `usedTrendAdjustment`

当前小程序调用方：

- `pages/health-profile`
- `pages/onboarding-profile`

### 5. 查询每日汇总

`GET /api/users/{userId}/daily-summary?date=2026-03-07`

关键返回字段：

- `targetCalories`
- `dietCalories`
- `exerciseCalories`
- `netCalories`
- `remainingCalories`
- `records`
- `healthDiary`

当前小程序调用方：

- `pages/home`

### 6. 查询热量进度摘要

`GET /api/users/{userId}/progress?startDate=2026-03-01&endDate=2026-03-07`

说明：

- 返回结构中的 `averageCalories`、`totalCalories` 与 `trend[].consumedCalories` 当前均表示净摄入热量口径。

当前小程序调用方：

- `services/user.js`

## 食物与饮食记录

### 7. 查询食物列表

`GET /api/foods?keyword=米饭`

可选参数：

- `category`
- `scope=ALL|CUSTOM`
- `page`
- `size`

当前小程序调用方：

- `pages/food-search`
- `pages/custom-food`
- `pages/meal-combo-manage`

### 8. 创建自定义食物

`POST /api/foods`

当前小程序调用方：

- `pages/custom-food`

### 9. 更新自定义食物

`PUT /api/foods/{id}`

当前小程序调用方：

- `pages/custom-food`

### 10. 删除自定义食物

`DELETE /api/foods/{id}`

当前小程序调用方：

- `pages/custom-food`

### 11. 创建饮食记录

`POST /api/records`

请求示例：

```json
{
  "foodId": 1,
  "mealType": "LUNCH",
  "quantityInGram": 180,
  "recordDate": "2026-03-07"
}
```

当前小程序调用方：

- `pages/food-search`

### 12. 批量创建饮食记录

`POST /api/records/batch`

说明：

- 主要用于套餐加入当前餐次时一次保存多条饮食记录。

当前小程序调用方：

- `pages/food-search`

### 13. 查询某日饮食记录

`GET /api/records?date=2026-03-07&mealType=LUNCH`

说明：

- `userId` 参数可选；有 token 时由后端自动识别。

当前小程序调用方：

- `pages/home`
- `pages/food-search`

### 14. 更新饮食记录

`PUT /api/records/{id}`

当前小程序调用方：

- `pages/food-search`

### 15. 删除饮食记录

`DELETE /api/records/{id}`

当前小程序调用方：

- `pages/food-search`

## 运动与运动记录

### 16. 查询运动列表

`GET /api/exercises?keyword=run&category=CARDIO`

可选参数：

- `category`
- `scope=ALL|CUSTOM`

当前小程序调用方：

- `pages/exercise-search`
- `pages/custom-exercise`

### 17. 创建自定义运动

`POST /api/exercises`

### 18. 更新自定义运动

`PUT /api/exercises/{id}`

### 19. 删除自定义运动

`DELETE /api/exercises/{id}`

当前小程序调用方：

- `pages/custom-exercise`

### 20. 创建运动记录

`POST /api/exercise-records`

请求示例：

```json
{
  "exerciseId": 12,
  "durationMinutes": 30,
  "intensityLevel": "MEDIUM",
  "recordDate": "2026-03-07"
}
```

当前小程序调用方：

- `pages/exercise-search`

### 21. 查询某日运动记录

`GET /api/exercise-records?date=2026-03-07`

当前小程序调用方：

- `pages/home`
- `pages/exercise-search`

### 22. 更新运动记录

`PUT /api/exercise-records/{id}`

当前小程序调用方：

- `pages/exercise-search`

### 23. 删除运动记录

`DELETE /api/exercise-records/{id}`

当前小程序调用方：

- `pages/exercise-search`

## 套餐、体征与健康日记

### 24. 查询套餐列表

`GET /api/meal-combos?userId=1`

### 25. 查询套餐详情

`GET /api/meal-combos/{id}?userId=1`

### 26. 创建套餐

`POST /api/meal-combos`

### 27. 更新套餐

`PUT /api/meal-combos/{id}?userId=1`

### 28. 删除套餐

`DELETE /api/meal-combos/{id}?userId=1`

当前小程序调用方：

- `pages/meal-combo-manage`
- `pages/food-search`

### 29. 查询体征快照

`GET /api/body-metrics/snapshot`

关键返回字段：

- `metricKey`
- `latestValue`
- `latestRecordDate`

当前小程序调用方：

- `pages/progress`

### 30. 新增体征记录

`POST /api/body-metrics`

关键请求字段：

- `metricKey`
- `metricValue`
- `recordDate`
- `metricUnit`

当前小程序调用方：

- `pages/progress`
- `pages/metric-history`

### 31. 查询体征趋势

`GET /api/body-metrics/trend?metricKey=WEIGHT&rangeType=MONTH`

可选参数：

- `cursorDate`
- `cursorId`
- `pageSize`

当前小程序调用方：

- `pages/progress`

### 32. 查询体征历史明细

`GET /api/body-metrics/history?metricKey=WEIGHT`

可选参数：

- `cursorDate`
- `cursorId`
- `pageSize`

当前小程序调用方：

- `pages/metric-history`

### 33. 查询某日健康日记

`GET /api/health-diaries/daily?date=2026-03-11`

响应（无记录时 `data` 为 `null`）：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "id": 12,
    "userId": 1,
    "recordDate": "2026-03-11",
    "content": "今天饮食节奏稳定，状态不错。",
    "imageFileIds": [
      "cloud://xxx/health-diary/1/2026-03-11/a.jpg"
    ],
    "createdAt": "2026-03-11T10:20:30",
    "updatedAt": "2026-03-11T10:20:30"
  },
  "timestamp": "2026-03-11T10:20:30"
}
```

当前小程序调用方：

- `pages/home`
- `pages/health-diary-editor`

### 34. 保存某日健康日记

`PUT /api/health-diaries/daily`

请求：

```json
{
  "recordDate": "2026-03-11",
  "content": "今天饮食节奏稳定，状态不错。",
  "imageFileIds": [
    "cloud://xxx/health-diary/1/2026-03-11/a.jpg"
  ]
}
```

当前小程序调用方：

- `pages/health-diary-editor`

### 35. 删除某日健康日记

`DELETE /api/health-diaries/daily?date=2026-03-11`

当前小程序调用方：

- `pages/health-diary-editor`

## 小程序接入建议

- `app.onLaunch` 先执行 `wx.login -> /api/auth/wechat/login`，保存 `accessToken + userId`。
- 请求层统一自动附带 `Authorization`，401 时自动重登一次。
- 首页调用 `/api/users/{id}/daily-summary` 展示饮食摄入、运动消耗和净摄入。
- 首页记录链路分两条入口：饮食记录（`/api/records`）与运动记录（`/api/exercise-records`）。
- 趋势页调用 `/api/body-metrics/snapshot` 和 `/api/body-metrics/trend` 展示体征变化。
- 首页在记录区下方展示”健康日记”入口与当日摘要，点击进入独立编辑页。

## 数据导入

### 36. 预览体重数据导入

`POST /api/body-metrics/import/preview`

说明：

- 接收 CSV / XLSX 文件内容，解析后返回预览数据，**不写入数据库**。
- 自动检测文件类型、分隔符（CSV）、日期列和体重列、日期格式、单位（kg/斤/lbs）。
- 支持带时间的日期文本，解析后仅保留日期部分，例如 `2026/3/28 09:39` 最终按 `2026-03-28` 处理。
- 单位从体重列名中提取（如 `体重 (斤)` → 斤，`Weight (lbs)` → lbs），并自动转换为 kg。

请求：

```json
{
  “fileName”: “export.xlsx”,
  “fileBase64”: “base64-encoded-file-content”
}
```

关键返回字段：

- `totalRows` — 数据行总数（不含表头）
- `parsedRows` — 成功解析的行数
- `skippedRows` — 无法解析的行数
- `detectedFileType` — 检测到的文件类型（`CSV` / `XLSX`）
- `detectedSheetName` — 检测到的工作表名称（仅 XLSX 返回）
- `detectedDelimiter` — 检测到的分隔符（`,` / `;` / `Tab`）
- `detectedUnit` — 检测到的单位（`kg` / `斤` / `lbs`）
- `rows[].rawDate` — 原始日期文本
- `rows[].rawWeight` — 原始体重文本
- `rows[].parsedDate` — 解析后的日期（解析失败为 null）
- `rows[].parsedWeightKg` — 解析后的体重 kg（解析失败为 null）
- `rows[].error` — 错误信息（无错误为 null）

当前小程序调用方：

- `pages/weight-import`

### 37. 确认体重数据导入

`POST /api/body-metrics/import/confirm`

说明：

- 将预览确认的数据写入数据库。
- `duplicatePolicy` 为 `SKIP` 时跳过已有记录，为 `OVERWRITE` 时覆盖已有记录。
- 如果导入数据包含今日日期，会同步更新用户当前体重和目标方案。
- 单次最多 1000 条。

请求：

```json
{
  “rows”: [
    { “date”: “2025-01-01”, “weightKg”: 70.5 },
    { “date”: “2025-01-02”, “weightKg”: 71.0 }
  ],
  “duplicatePolicy”: “SKIP”
}
```

关键返回字段：

- `imported` — 成功导入数
- `skipped` — 跳过数
- `overwritten` — 覆盖数
- `errors` — 错误信息列表

当前小程序调用方：

- `pages/weight-import`
