spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/diet?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:xinqi_diet}
    password: ${DB_PASSWORD:XinqiDiet@2026!}

logging:
  level:
    com.diet: debug

wechat:
  app-id: ${WECHAT_APP_ID:}
  app-secret: ${WECHAT_APP_SECRET:}
  mock-enabled: ${WECHAT_MOCK_ENABLED:true}

tencent:
  ocr:
    secret-id: ${TENCENT_OCR_SECRET_ID:}
    secret-key: ${TENCENT_OCR_SECRET_KEY:}
    region: ${TENCENT_OCR_REGION:ap-guangzhou}
    mock-enabled: ${TENCENT_OCR_MOCK_ENABLED:true}

auth:
  token-secret: ${AUTH_TOKEN_SECRET:diet-dev-secret-change-me}
  token-expire-days: ${AUTH_TOKEN_EXPIRE_DAYS:30}
